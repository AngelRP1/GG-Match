name: CI/CD Pipeline GG-Match

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar código
        uses: actions/checkout@v3

      - name: Instalar CLI de DigitalOcean
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Iniciar sesión en el Container Registry
        run: doctl registry login --expiry-seconds 600

      - name: Construir imagen Auth
        run: docker build -t registry.digitalocean.com/gg-match-registry/gg-match-api:latest ./auth-service

      - name: Subir imagen Auth
        run: docker push registry.digitalocean.com/gg-match-registry/gg-match-api:latest

      - name: Construir imagen Tournament
        run: docker build -t registry.digitalocean.com/gg-match-registry/gg-match-tournament:latest ./tournament-service

      - name: Subir imagen Tournament
        run: docker push registry.digitalocean.com/gg-match-registry/gg-match-tournament:latest

      - name: Desplegar en el Droplet
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            docker login registry.digitalocean.com -u ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }} -p ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
            cd ~/GG-Match
            
            # Descargar ambas imágenes frescas
            docker pull registry.digitalocean.com/gg-match-registry/gg-match-api:latest
            docker pull registry.digitalocean.com/gg-match-registry/gg-match-tournament:latest
            
            # Reiniciar todo
            docker-compose up -d